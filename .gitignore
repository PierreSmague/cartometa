# Source content copied from Plonk It — personal use, never versioned
input/

# Heavy derived data (cropped images, caches)
data/cache/
data/images/

# Python
.venv/
__pycache__/
*.pyc

# Plonk It texts and images — owner decision 2026-07-29
# Regenerable with: uv run cartometa-extract poland
data/metas/

# Base map experiments: directory deleted on 2026-08-04, the key now lives in
# the CARTOMETA_GOOGLE_KEY environment variable. The rule stays as a precaution
# — the repository is public, and a `git add data` would publish any key put
# back here.
data/background/

# Logs of the scheduled task
logs/

# Process artefacts
.superpowers/

# Build artefact of the public site
dist/
# Viewer data: regenerated by cartometa-build, never versioned
viewer/data/

# Local state of wrangler (the Cloudflare CLI)
.wrangler/
